export const SET_COUNTRY_LIST = (state, list) => {
  state.countryList = list;
};
export const SET_AVALABLE_LIST = (state, list) => {
  state.avalDatesList = list;
};

export const SET_AVALABLE_TIMING = (state, list) => {
  state.availableTimings = list;
};
